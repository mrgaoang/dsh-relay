# 安全策略

dsh-relay 提供公网可达的信令/中继服务,安全问题优先处理。

## 报告漏洞

**不要**公开披露漏洞(不要在 issue/讨论区贴细节)。

请发邮件至:`<security@你的域名>`(维护者请替换为真实邮箱),包含:

- 影响组件(relay-core / relay-free / 协议 / 文档)
- 复现步骤与 PoC(如有)
- 影响评估(数据泄露?越权?拒绝服务?)

## 承诺

- 确认漏洞后尽快修复,并发布安全公告(CHANGELOG 中标注 `[security]`)
- 报告者会得到致谢(除非要求匿名)

## 已知安全设计

| 面 | 设计 |
|---|---|
| 账号密码 | scrypt(N=16384,r=8,p=1,keyLen=64),与商业版同格式 |
| 会话 | JWT HMAC-SHA256,2h 过期,`jti` 注销 |
| 传输 | 账号 API 必须走 HTTPS/WSS;`install-client` 拒绝非回环明文 |
| 设备身份 | 客户端 ed25519 密钥对,私钥不出设备 |
| 数据面 | WebRTC DTLS 端到端加密,relay 只转发信令 |
| TURN(商业版) | HMAC-SHA1 临时凭证,1h TTL,按套餐配额 |

## 报告处理时间线

- 24h 内确认收到
- 高危:72h 内给出修复计划
- 修复后公开致谢
